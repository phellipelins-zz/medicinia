import { NotificationFilterPipe } from './notification-filter.pipe';

describe('NotificationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
